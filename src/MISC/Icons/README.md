# Shortcut Icons

Place custom shortcut icon files here.

- Default path used by the shortcut task: `src/MISC/Icons/CITA-LabTools.ico`
- Configure in: `src/config/labtools.json` -> `shortcuts.iconRelativePath`
- Recommended format: `.ico`
